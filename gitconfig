[user]
	name = Thomas Bonnin
email = ***REMOVED***

[github]
user = TBonnin	

[color]
diff = auto
status = auto
branch = auto
interactive = auto
ui = true
pager = true

[color "branch"]
current = yellow reverse
local = yellow
remote = green

[color "diff"]
meta = yellow bold
frag = magenta bold
old = red bold
new = green bold

[color "status"]
added = yellow
changed = green
untracked = cyan

[core]
pager = less -FRSX
whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol
excludesfile = ~/.gitignore
editor = vim
autocrlf = false
native = true

[alias]
st = status
ci = commit
co = checkout
br = branch
w = whatchanged
lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
uncommit = reset --soft HEAD^
d = diff HEAD
b = browse
bi = browse -- issues
branch-name = !git branch --no-color 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \\(.*\\)/\\1/'
pullc = !git pull origin `git branch-name`
pushc = !git push origin `git branch-name`
ct = "!f() { git fetch && git co -b $1 origin/$1; }; f"

[diff]  
renamelimit = 0
renames = true

[merge]
summary = true
keepBackup = false
keepTemporaries = false
	tool = opendiff

[mergetool]
keepBackup = false
keepTemporaries = false
trustExitCode = false


